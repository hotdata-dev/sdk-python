# coding: utf-8

"""
    Hotdata API

    Powerful data platform API for managed databases, queries, and analytics.

    The version of the OpenAPI document: 1.0.0
    Contact: developers@hotdata.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class UploadSessionResponse(BaseModel):
    """
    A created upload session: everything needed to upload the bytes directly to storage and later finalize the upload.
    """ # noqa: E501
    finalize_token: StrictStr = Field(description="One-time token that authorizes finalizing this upload. Returned exactly once at create time — store it; it cannot be retrieved again.")
    headers: Dict[str, StrictStr] = Field(description="Headers you must send verbatim with each `PUT`. Currently always empty; present so a future mode can require signed headers without changing the response shape.")
    mode: StrictStr = Field(description="Upload mode: `single` (upload the whole file with one `PUT` to `url`) or `multipart` (upload each part with one `PUT` to the matching entry in `part_urls`). Modeled as a string so additional modes can be added later without breaking clients.")
    part_size: Optional[Annotated[int, Field(strict=True, ge=0)]] = Field(default=None, description="For a `multipart` upload, the size in bytes to split the file into: send bytes `[(i-1) * part_size, i * part_size)` to `part_urls[i - 1]`, with the last part carrying the remainder. Slice by this value — do **not** divide the file evenly by `part_urls.len()`, which can make a non-final part smaller than the 5 MiB minimum that storage requires (the upload then fails at finalize). Absent for `single` uploads.")
    part_urls: Optional[List[StrictStr]] = Field(default=None, description="For a `multipart` upload, the per-part URLs in ascending part order: `PUT` your file's part *i* (1-based) to `part_urls[i - 1]` and keep each response's `ETag`, then pass the `{part_number, e_tag}` list to finalize. Absent for `single` uploads.")
    upload_id: StrictStr = Field(description="Identifier for this upload. Pass it to the finalize endpoint and to the managed-table load endpoint once finalized.")
    url: Optional[StrictStr] = Field(default=None, description="The URL to `PUT` the raw file bytes to, for a `single` upload. Short-lived — upload promptly and finalize. Absent for `multipart` uploads (use `part_urls`).")
    __properties: ClassVar[List[str]] = ["finalize_token", "headers", "mode", "part_size", "part_urls", "upload_id", "url"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of UploadSessionResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if part_size (nullable) is None
        # and model_fields_set contains the field
        if self.part_size is None and "part_size" in self.model_fields_set:
            _dict['part_size'] = None

        # set to None if part_urls (nullable) is None
        # and model_fields_set contains the field
        if self.part_urls is None and "part_urls" in self.model_fields_set:
            _dict['part_urls'] = None

        # set to None if url (nullable) is None
        # and model_fields_set contains the field
        if self.url is None and "url" in self.model_fields_set:
            _dict['url'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of UploadSessionResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "finalize_token": obj.get("finalize_token"),
            "headers": obj.get("headers"),
            "mode": obj.get("mode"),
            "part_size": obj.get("part_size"),
            "part_urls": obj.get("part_urls"),
            "upload_id": obj.get("upload_id"),
            "url": obj.get("url")
        })
        return _obj


